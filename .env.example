# ===== Frontend Environment Variables =====

# Backend API URL
VITE_API_URL=http://localhost:3001/api

# Google Apps Script Web App URL for syncing data TO Google Sheets (optional)
# Get this from deploying your Google Apps Script as a web app
VITE_GOOGLE_APPS_SCRIPT_URL=https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec

# Google Sheets API Key for importing data FROM Google Sheets (optional)
# Get this from Google Cloud Console - APIs & Services > Credentials
VITE_GOOGLE_SHEETS_API_KEY=your_google_sheets_api_key_here

# Firebase Configuration
# Get these from Firebase Console > Project Settings > General > Your apps
VITE_FIREBASE_API_KEY=your_firebase_api_key
VITE_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id
VITE_FIREBASE_APP_ID=your_firebase_app_id

# ===== Backend Environment Variables =====
# Copy these to your .env file in the root directory

# PostgreSQL Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=onboarding_tracker
DB_USER=postgres
DB_PASSWORD=your_password_here

# Server Configuration
PORT=3001
NODE_ENV=development

# Firebase Admin SDK Configuration (for backend authentication)
# Download service account JSON from Firebase Console > Project Settings > Service Accounts
# Save it to your project and set the path here (e.g., ./firebase-service-account.json)
FIREBASE_SERVICE_ACCOUNT_PATH=./firebase-service-account.json

# Setup Instructions:
# 1. For Firebase Configuration:
#    Frontend (VITE_FIREBASE_*):
#    - Go to Firebase Console (console.firebase.google.com)
#    - Create a new project or select existing
#    - Go to Project Settings > General > Your apps
#    - Add a web app and copy the config values
#
#    Backend (FIREBASE_SERVICE_ACCOUNT_PATH):
#    - Go to Firebase Console > Project Settings > Service Accounts
#    - Click "Generate new private key"
#    - Save the JSON file to your project directory
#    - Update the path in FIREBASE_SERVICE_ACCOUNT_PATH
#
# 2. For VITE_GOOGLE_APPS_SCRIPT_URL:
#    - Open Google Apps Script (script.google.com)
#    - Create a new project and paste the code from google-apps-script-manual-headers.js
#    - Deploy as Web App with "Anyone" access
#    - Copy the web app URL here
#
# 3. For VITE_GOOGLE_SHEETS_API_KEY:
#    - Go to Google Cloud Console (console.cloud.google.com)
#    - Create a new project or select existing
#    - Enable Google Sheets API
#    - Create an API Key credential
#    - Copy the API key here
#
# 4. Rename this file to .env (remove .example)
# 5. Restart your development server after adding these variables